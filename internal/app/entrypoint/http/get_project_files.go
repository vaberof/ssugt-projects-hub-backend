package http

import (
	"github.com/gin-gonic/gin"
)

func (handler *Handler) GetProjectFiles(ctx *gin.Context) {
	/*projectId := ctx.Param("projectId")

	ctx.JSON(http.StatusOK, apiv1.Success(payload))*/
}
